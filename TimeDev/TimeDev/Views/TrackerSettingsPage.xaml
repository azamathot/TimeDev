<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TimeDev.Views.TrackerSettingsPage"
             BackgroundColor="{DynamicResource PageBackgroundColor}"
             xmlns:vm="clr-namespace:TimeDev.ViewModels" 
             x:DataType="vm:AppSettingsViewModel"
             xmlns:resx="clr-namespace:TimeDev.Resx"
             >
    <ContentPage.Content>
        <Grid Margin="10" Padding=" 10">
            <StackLayout Padding="9" VerticalOptions="Start">
                <FlexLayout Direction="Row" JustifyContent="SpaceAround" AlignItems="Center">
                    <Picker ItemsSource="{Binding AppSettings.UserSettings.InstanceList}" ItemDisplayBinding="{Binding Instance}" SelectedItem="{Binding SelectedInstance}" SelectedIndex="{Binding AppSettings.UserSettings.SelectedInstanceIndex}" FlexLayout.Basis="89%"/>
                    <Button Text="+" FontSize="Large" FontAttributes="Bold" Command="{Binding AddInstanceCommand}" FlexLayout.Basis="10%"/>
                </FlexLayout>
                <FlexLayout Direction="Row" JustifyContent="SpaceAround" AlignItems="Center">
                    <Label Text="{x:Static resx:Resource.TrackerType}" FlexLayout.Basis="25%"/>
                    <Picker ItemsSource="{Binding AppSettings.BaseSettings.TrackerTypeList}" SelectedItem="{Binding SelectedInstance.TrackerType}" FlexLayout.Basis="70%"/>
                </FlexLayout>
                <FlexLayout Direction="Row" JustifyContent="SpaceAround" AlignItems="Center">
                    <Label Text="{x:Static resx:Resource.APIkey}" FlexLayout.Basis="25%"/>
                    <Entry Text="{Binding SelectedInstance.APIkey}" FlexLayout.Basis="70%"/>
                </FlexLayout>
                <FlexLayout Direction="Row" JustifyContent="SpaceAround" AlignItems="Center">
                    <Label Text="{x:Static resx:Resource.GroupBy}" FlexLayout.Basis="25%"/>
                    <Picker ItemsSource="{Binding AppSettings.BaseSettings.GroupByList}" SelectedItem="{Binding SelectedInstance.GroupBy}" FlexLayout.Basis="70%"/>
                </FlexLayout>
                <FlexLayout Direction="Row" JustifyContent="SpaceAround" AlignItems="Center">
                    <Label Text="{x:Static resx:Resource.SortBy}" FlexLayout.Basis="25%"/>
                    <Picker ItemsSource="{Binding AppSettings.BaseSettings.SortByList}" SelectedItem="{Binding SelectedInstance.SortBy}" FlexLayout.Basis="70%"/>
                </FlexLayout>
            </StackLayout>
            <StackLayout Padding="10" VerticalOptions="End" >
                <FlexLayout Direction="Row" JustifyContent="SpaceAround">
                    <Button Text="{x:Static resx:Resource.Save}" Command="{Binding SaveSettingsCommand}" FlexLayout.Basis="33%" CornerRadius="7"/>
                    <Button Text="{x:Static resx:Resource.Check}" Command="{Binding CheckSettingsCommand}" FlexLayout.Basis="33%" CornerRadius="7"/>
                    <Button Text="{x:Static resx:Resource.Cancel}" Command="{Binding CancelChangeCommand}" FlexLayout.Basis="33%" CornerRadius="7"/>
                </FlexLayout>
            </StackLayout>
        </Grid>
    </ContentPage.Content>
</ContentPage>